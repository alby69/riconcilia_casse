{
  "common": {
    "comment": "Parametri comuni a tutti gli algoritmi",
    "column_mapping": {
      "Data": "Date",
      "Dare": "Debit",
      "Avere": "Credit",
      "INCASSI": "Debit",
      "VERSAMENTI": "Credit"
    },
    "input_folder": "input",
    "output_folder": "output",
    "file_patterns": ["*.xlsx", "*.csv"],
    "algorithm": "auto",
    "residual_threshold": 100.0,
    "residual_days_window": 90
  },
  "subset_sum": {
    "comment": "Parametri specifici per l'algoritmo subset_sum",
    "tolerance": 0.5,
    "days_window": 15,
    "max_combinations": 8,
    "search_direction": "both"
  },
  "progressive_balance": {
    "comment": "Parametri specifici per l'algoritmo progressive_balance. Tolleranza molto bassa, solo futuro.",
    "tolerance": 0.01,
    "days_window": 7,
    "search_direction": "future_only",
    "ignore_tolerance": false
  },
  "optimizer": {
    "comment": "Impostazioni per il processo di ottimizzazione dei parametri.",
    "range_percentage": 0.3,
    "n_trials_first_run": 100,
    "n_trials_refinement": 50,
    "parameter_space": {
        "algorithm": {"type": "categorical", "values": ["subset_sum", "progressive_balance"]},
        "days_window": {"type": "numeric", "min": 1, "max": 20},
        "tolerance": {"type": "numeric", "min": 0.01, "max": 2.0, "step": 0.01, "value_type": "float"},
        "max_combinations": {"type": "numeric", "min": 2, "max": 10},
        "search_direction": {"type": "categorical", "values": ["past_only", "future_only", "both"]},
        "sorting_strategy": {"type": "categorical", "values": ["date", "amount"]}
    }
  }
}